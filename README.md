
  # iPhone Price Comparison

  This is a code bundle for iPhone Price Comparison. The original project is available at https://www.figma.com/design/zSViNjTin0XL1FOe6u2vFn/iPhone-Price-Comparison.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  